{
  "installCommand": "cd .. && npm install --legacy-peer-deps",
  "buildCommand": "cd ../crn-shared && npm run build && cd ../crn-api && npx prisma generate && if [ \"$PREVIEW_AUTO_MIGRATE\" = \"1\" ] && [ \"$VERCEL_ENV\" = \"preview\" ]; then npx prisma migrate deploy; fi && npx next build",
  "outputDirectory": ".next",
  "crons": [
    {
      "path": "/api/cron/sync-calendars",
      "schedule": "0 * * * *"
    }
  ]
}
