# Duplicated from the monorepo root .npmrc: npm does not inherit project
# config from parent directories, and EAS Build runs npm ci inside this
# directory (lucide-react-native peers React 16-18; Expo 53 ships React 19).
legacy-peer-deps=true
